#!/bin/bash

echo -e "\e[32mUpdate Omarchy\e[0m"
git -C $OMARCHY_PATH pull basecamp --autostash
git -C $OMARCHY_PATH pull origin --autostash
git -C $OMARCHY_PATH diff --check || git -C $OMARCHY_PATH reset --merge
