# Employee-only Hyprland bindings.
#
# Keep Omarchy defaults in bindings.conf and Pondhouse defaults in
# pondhouse-custom.conf. Put private machine/user overrides here. Before each
# bind line, unbind the same key first to avoid duplicate binds after updates.
